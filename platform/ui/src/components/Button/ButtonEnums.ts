enum type {
  primary = 'primary',
  secondary = 'secondary',
}
enum size {
  large = 'large',
  medium = 'medium',
  small = 'small',
}

enum orientation {
  horizontal = 'horizontal',
  vertical = 'vertical',
}

export { type, size, orientation };
